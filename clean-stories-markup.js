'use strict';
(() => {
  const S = window.S52;
  S.storyArchiveMarkup = ({ visible, pages }) => `<section class="surface"><div class="screen-heading"><p class="eyebrow">52 STORIE PRONTE</p><h2>Scegliete la prima scena.</h2><p>Dopo la scelta leggerete gli obiettivi segreti e inizierete dalla storia selezionata.</p></div><div class="story-toolbar"><input type="search" data-story-search value="${S.esc(S.storyUi.query)}" placeholder="Cerca titolo, luogo o personaggio"><button type="button" class="secondary" data-random-story>Casuale</button></div><div class="category-grid"><button type="button" class="${S.storyUi.category === 'all' ? 'active' : ''}" data-category="all">Tutte<small>52</small></button>${Object.entries(S.categories).map(([key, category]) => `<button type="button" class="${S.storyUi.category === key ? 'active' : ''}" data-category="${key}">${S.esc(category.symbol)} ${S.esc(category.label)}<small>13</small></button>`).join('')}</div><div class="story-list">${visible.map(story => `<article class="ready-story"><div class="meta"><span>${S.esc(S.categories[story.category]?.symbol || '')} ${S.esc(S.categories[story.category]?.label || '')}</span><span>${String(S.stories.indexOf(story) + 1).padStart(2, '0')}/52</span></div><h3>${S.esc(story.title)}</h3><p>${S.esc(story.opening)}</p><button type="button" data-ready-story="${S.esc(story.id)}">Scegli questa storia</button></article>`).join('') || '<div class="hint">Nessuna storia trovata.</div>'}</div><div class="pagination"><button type="button" data-page-prev${S.storyUi.page === 0 ? ' disabled' : ''}>← Precedenti</button><span>${S.storyUi.page + 1} / ${pages}</span><button type="button" data-page-next${S.storyUi.page >= pages - 1 ? ' disabled' : ''}>Successive →</button></div></section>`;
})();
